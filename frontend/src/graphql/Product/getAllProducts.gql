query GetAllProducts($limit: Int, $offset: Int) {
  getAllProducts(limit: $limit, offset: $offset) {
    products {
      id
      name
      price
      description
      thumbnail
      brand {
        id
        name
      }
      category {
        id
        name
      }
    }
    total
  }
}
