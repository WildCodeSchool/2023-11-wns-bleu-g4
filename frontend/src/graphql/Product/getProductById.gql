query GetProductById($productId: Int!) {
  getProductById(productId: $productId) {
    id
    name
    price
    description
    thumbnail
    category {
      id
      name
    }
    brand {
      id
      name
    }
    pictures {
      thumbnail
      alt
      id
    }
    characteristics {
      id
      characteristic
    }
  }
}
