query GetAllBooking($offset: Int, $limit: Int) {
  getAllBooking(offset: $offset, limit: $limit) {
    bookings {
      id
      status
      invoice
      bookingDate
      startDate
      endDate
    }
    total
  }
}