query GetAllBooking($limit: Int, $offset: Int) {
  getAllBooking(limit: $limit, offset: $offset) {
    bookings {
      id
      status
      invoice
      bookingDate
      startDate
      endDate
    }
    total
  }
}
